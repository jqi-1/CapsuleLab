# Reproducibility Report

## Setup

Run:

```bash
cap doctor
cap build
cap start
cap app start jupyter
```

## Inputs

- Source notes: `papers/sources.md`
- Data directory: `data/`
- Model/cache directory: `models/`

## Evaluation

Record query sets, retrieval configuration, and metrics in `experiments/evaluation_runs.md`.
