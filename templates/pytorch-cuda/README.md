# My Project

Created with CapsuleLab (PyTorch CUDA template).

## Quick Start

```bash
cap doctor
cap build
cap start
cap app start jupyter
cap app open jupyter
```

## Structure

- `notebooks/` — Jupyter notebooks
- `src/` — Python source code
- `data/` — datasets
- `models/` — trained models
